{
  "version": 2,
  "framework": "vite",
  "installCommand": "npm ci --include=dev || npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}

